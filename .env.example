# Example environment configuration for Tariff Map
# Copy this file to `.env` (for public defaults) and/or `.env.local` (for private secrets).
# Values from real system environment variables always take precedence.

# === Flask ===
FLASK_ENV=development
# Use a strong random value in production (e.g. 32+ bytes hex)
SECRET_KEY=change-me-dev-secret
SESSION_COOKIE_HTTPONLY=1
# Set to 1 in production (HTTPS)
SESSION_COOKIE_SECURE=0
# Lax/Strict/None (None requires HTTPS)
SESSION_COOKIE_SAMESITE=Lax

# === Admin login (simple PoC auth) ===
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me

# === WTO Timeseries API ===
# Key from https://apiportal.wto.org/
# (Header: Ocp-Apim-Subscription-Key)
WTO_API_KEY=
WTO_DEFAULT_LANGUAGE=1       # 1=en, 2=fr, 3=es
WTO_DEFAULT_FORMAT=json

# === WITS (World Bank / UNCTAD TRAINS) ===
# Most endpoints are public. Provide only if you have a key.
WITS_API_KEY=
WITS_FALLBACK_YEAR=2021

# === Domain defaults ===
TOBACCO_CLASSIFICATION=HS
TOBACCO_PRODUCT_CODE=24
DEFAULT_YEAR=2023
